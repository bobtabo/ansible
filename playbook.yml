#
# Vagrantの設定
#
- hosts: all
  become: yes
  user: vagrant
  vars:
    db_name: hoge
    db_user: fuga
    db_password: hidebu
    laravel_home: /home/vagrant/laravel6
    app_name: hoge
    app_url: http://hoge.local
  roles:
    - common
    - php
    - nginx
    - mariadb
    - laravel
  tasks:
    - name: Gitのインストール
      yum:
        name: git
